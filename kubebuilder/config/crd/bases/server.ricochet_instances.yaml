---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: instances.server.ricochet
spec:
  group: server.ricochet
  names:
    kind: Instance
    listKind: InstanceList
    plural: instances
    singular: instance
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Instance is the Schema for the instances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstanceSpec defines the desired state of Instance
            properties:
              game:
                description: The game and modloader to be used for the server.
                properties:
                  modLoader:
                    default: vanilla
                    description: The software used to load mods into the game server.
                      Vanilla will launch the game server as default without any mods.
                    type: string
                  name:
                    description: The name of the game type to be created.
                    type: string
                required:
                - name
                type: object
              id:
                description: The unique identifier of the game server instance.
                type: string
              name:
                default: Hyperborea
                description: The name of the server.
                type: string
              network:
                description: The network configuration for the server.
                properties:
                  type:
                    description: The type of network to be used for the server.
                    enum:
                    - public
                    - private
                    type: string
                type: object
              size:
                default: xs
                description: 'This changes the resources given to the server and the
                  player limit. Valid values are: xs, s, m, l, xl'
                enum:
                - xs
                - s
                - m
                - l
                - xl
                type: string
            required:
            - game
            - id
            type: object
          status:
            description: InstanceStatus defines the observed state of Instance
            properties:
              id:
                type: string
              name:
                type: string
              size:
                type: string
            required:
            - id
            - name
            - size
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
